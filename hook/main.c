#include <stdio.h>
#include <unistd.h>
#include <string.h>
#include "func_hook.h"

int main(int arc, char*argv[]){

    /*
    const char *msg = "Am I hooked?\n";
    write(STDOUT_FILENO, msg, strlen(msg));//call whitch write?
    return 0;

    sleep(1000);

    char * pid= "86483";
    char buff[1024] = {0};
    process_name(argv[1], buff);
    printf("buff:%s\n", buff);

    */
    while(1){}

    return 0;
}
